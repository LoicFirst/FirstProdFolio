# Test Environment Variables Template
# Copy this file to .env.local and fill in your actual values

# Base de données MongoDB
# Format recommandé: mongodb+srv://username:password@cluster.mongodb.net/?appName=ClusterName
# IMPORTANT: Remplacez les valeurs suivantes par vos vraies valeurs MongoDB Atlas:
#   - YOUR_USER: votre nom d'utilisateur MongoDB (ex: myuser)
#   - YOUR_PASSWORD: votre mot de passe MongoDB (ex: mypass123 - PAS <password> ou YOUR_PASSWORD)
#   - YOUR_CLUSTER: votre adresse de cluster (ex: cluster0.abc123)
#
# Pour obtenir votre chaîne de connexion:
# 1. Connectez-vous à MongoDB Atlas (https://cloud.mongodb.com)
# 2. Allez dans Database > Connect > Drivers
# 3. Copiez la chaîne de connexion et remplacez <password> par votre mot de passe réel
#
# Exemple valide:
# MONGODB_URI=mongodb+srv://myuser:SecurePass123@cluster0.abc123.mongodb.net/?appName=Cluster0
#
# ⚠️ NE PAS UTILISER de placeholders comme <password>, YOUR_PASSWORD, ou password
# ⚠️ Assurez-vous que la liste blanche IP est configurée dans MongoDB Atlas (Network Access)
MONGODB_URI=mongodb+srv://YOUR_USER:YOUR_PASSWORD@YOUR_CLUSTER.mongodb.net/?appName=Cluster0

# NextAuth.js (authentification)
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=YOUR_GENERATED_SECRET_HERE
NEXTAUTH_URL=http://localhost:3000

# Identifiants admin
# Email must be a valid email format
# Password must be at least 8 characters
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=YourSecurePassword123
ADMIN_NAME=Admin User

# Cloudinary (upload d'images) - Optional for basic testing
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret
